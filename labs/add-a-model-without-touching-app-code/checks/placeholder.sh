#!/usr/bin/env bash
# Placeholder for part 2. Deliberately fails, so `labs test`'s "at least
# one check must fail on a fresh workspace" rule holds until real,
# outcome-based checks replace this file.
set -uo pipefail
echo '{"pass": false, "message": "placeholder check: part 2 replaces this with an outcome-based grader"}'
exit 1
