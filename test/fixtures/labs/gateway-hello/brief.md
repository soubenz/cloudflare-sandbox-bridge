# Brief

This lab exists to prove the gateway container image works: Prometheus and
Grafana both come up, both answer through the session service proxy, and the
image's load/LLM tooling (locust, litellm) is present.
