# Working in this repo

- Do all work on the `dev-landing-page` branch and open a pull request to
  `main`. Never push or merge to `main` directly; the owner merges.
- Pushes to `main` deploy to production. Pull requests that touch `site/`
  get a Cloudflare preview URL instead (see `site/README.md`).
